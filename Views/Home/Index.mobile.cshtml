@model Hurtownia.ViewModels.HomeViewModel

@{
    ViewBag.Title = "Index";
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <title>@ViewBag.Title</title>
    <meta name="viewport" content="width=device-width" />

    <link rel="stylesheet" href="//code.jquery.com/mobile/1.4.5/jquery.mobile-1.4.5.min.css" />
    <script src="//code.jquery.com/jquery-1.11.1.min.js"></script>
    <script src="//code.jquery.com/mobile/1.4.5/jquery.mobile-1.4.5.min.js"></script>

</head>
<body>
    @Html.Partial("_ViewSwitcher")
    <h3 class="ui-bar ui-bar-a">Witamy w  naszej hurtowni!</h3>

    <div class="ui-body">

        <ul data-role="listview" data-inset="true" data-divider-theme="a">

            <li data-role="list-divider">Nowości</li>

            @foreach (var newArrival in Model.NewArrivals)
            {
                <li>
                    <a href="@Url.Action("Details", "Store", new { id = newArrival.ProductId })" data-ajax="false">
                        <img src="@Url.Content("~/Content/Covers/" + newArrival.CoverFileName)">
                        <h2>@newArrival.ProductName</h2>
                        <p>@newArrival.ProductDescription</p>
                    </a>
                </li>
            }

            <li data-role="list-divider">Bestsellery</li>

            @foreach (var bestseller in Model.Bestsellers)
            {
                <li>
                    <a href="@Url.Action("Details", "Store", new { id = bestseller.ProductId })" data-ajax="false">
                        <img src="@Url.Content("~/Content/Covers/" + bestseller.CoverFileName)">
                        <h2>@bestseller.ProductName</h2>
                        <p>@bestseller.ProductDescription</p>
                    </a>
                </li>
            }

        </ul>

        <br />

        <ul data-role="listview" data-inset="true" data-filter-placeholder="Szukaj produktu..." data-filter="true">

            @foreach (var category in Model.Categories)
            {
                <li>
                    <a href="@Url.Action("List", "Store", new { categoryname = category.NameCategory.ToLower() })" data-ajax="false">@category.NameCategory</a>
                </li>
            }

        </ul>

        <br />

        <div data-role="footer">
            <div data-role="navbar">
                <ul>
                    <li><a href="https://localhost:44300/" data-ajax="false" data-icon="heart">Facebook</a></li>
                    <li><a href="@Url.Action("StaticContent", "Home", new { viewname = "Onas" })" data-ajax="false" data-icon="info">O nas</a></li>
                    <li><a href="@Url.Action("StaticContent", "Home", new { viewname = "Kontakt" })" data-ajax="false" data-icon="mail">Kontakt</a></li>
                </ul>
            </div><!-- /navbar -->
        </div><!-- /footer -->

    </div>


</body>
</html>
